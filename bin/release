#!/bin/bash
set -e

echo "==== release script starting ===="
appDir=$1
cacheDir=$2  #actually not set in this script, so CF docs are incorrect!
source ${appDir}/java_home.sh # sets JAVA_HOME
echo "appDir = " $appDir
echo "cacheDir = " $cacheDir

echo "=== env dump ===================="
set

echo "This script fails for now"
exit 1
